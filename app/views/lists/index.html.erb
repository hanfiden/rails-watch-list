<div class="container">
  <h1>All the lists</h1>

  <ul>
    <% @lists.each do |list| %>
      <li>
        <%= link_to list.name, list_path(list) %>
      </li>
    <% end %>
  </ul>

  <%= link_to "New Lists found", new_list_path, class: "btn btn-primary" %>
</div>
